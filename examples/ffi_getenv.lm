module FfiGetenv

import std.io.{println}

foreign "C" fn getenv(s: String) -> String

val main = {
    val p = getenv("PATH")
    println(p.len() > 0)
    val missing = getenv("LUMI_NO_SUCH_ENV_VAR_XYZ")
    println(missing.len())
}
