module mono_list
import lumi.io.{println}
import lumi.linalg.{zeros}

val head(xs) = {
    xs.get(0)
}

val main = {
    var xs = zeros(2)
    xs = xs.set(0, 1.25)
    xs = xs.set(1, 2.5)
    println(xs.get(0))
    println(head(xs))
}
