/// nested result.andThen Float then unwrapOr.
module main
import std.result.{andThen, unwrapOr}

val main = {
    val r = andThen(Ok(1.5), { x -> andThen(Ok(x), { y -> Ok(y * 2.0) }) })
    println(unwrapOr(r, 0.0))
}
