module PeAdtField

import lumi.io.{println}

type Point {
    val x
    val y
}

/// Const-fold product field projection from a literal AllocAdt (§7.5.1-A local).
val main = {
    val p = Point { x = 10, y = 20 }
    println(p.x)
    println(p.y)
    println(p.x + p.y)
}
