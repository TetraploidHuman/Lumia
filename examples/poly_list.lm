module PolyList

import lumi.io.{println}

/// Identity at List[Int] / List[Float] → `$List_Int` / `$List_Float`.
val id(x) = { x }

val main = {
    println(id(listOf(10, 20, 30)).get(1))
    println(id(listOf(1.5, 2.5)).get(1))
}
