module EffectHof

import std.io.{println}

/// Higher-order `apply` picks up IO from an effectful callback.
val apply(f, x) = f(x)

val boom(x) = {
    println(x)
    x + 1
}

val main = {
    println(apply(boom, 41))
}
