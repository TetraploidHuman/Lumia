[package]
name = "lumia_abi"
version.workspace = true
edition.workspace = true
description = "Shared C ABI for lumia_rt / codegen / opt: type_ids, memo caps, opt thresholds, scheduler kinds, dense_f64 trampoline symbols"

[lib]
path = "src/lib.rs"

[lints]
workspace = true
