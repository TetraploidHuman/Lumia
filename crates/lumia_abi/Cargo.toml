[package]
name = "lumia_abi"
version.workspace = true
edition.workspace = true
description = "Shared C ABI for lumia_rt / codegen / opt (type_id, memo, opt_caps, dense_f64, scheduler)"

[lib]
path = "src/lib.rs"

[lints]
workspace = true
